{
  "name": "syntax-hub-services",
  "version": "1.0.0",
  "description": "SyntaxHub AI Microservice (FastAPI + Uvicorn + ChromaDB)",
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn main:app --reload --port 8000",
    "start": ".venv/bin/python -m uvicorn main:app --port 8000"
  }
}
